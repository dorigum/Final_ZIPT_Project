# ZIPT 개발 로그 — 2026-06-23

이 문서는 2026-06-23에 작성된 ZIPT 프로젝트 로그 및 트러블슈팅 내역입니다.

---

### 주제: 백엔드 부동산 용어 사전(Term) API 및 도메인 개발


### 개요
부동산 용어 사전(Term) 관련 도메인 엔티티, Repository, Service, Controller 및 초기 데이터 로더를 신규 구축하였으며, API 응답 필드 수정 및 예외 처리, Null-safety 처리 보완 등 버그 수정 작업을 완료하여 백엔드 feature/terms 브랜치에 커밋 및 푸시를 완료하였다.

---

### 상세 작업 내역

1. 부동산 용어(Term) 도메인 엔티티 설계 부동산 용어 정보를 담을 RealEstateTerm 엔티티와 용어의 위험 등급을 나타내는 RiskLevel Enum, 그리고 용어 카테고리를 분류하는 TermCategory Enum을 신규 구현하였다.
2. 데이터베이스 연동용 Repository 구축 Spring Data JPA 기반의 RealEstateTermRepository 인터페이스를 신규 작성하여 데이터베이스 연동 및 기본 CRUD 기능을 활성화하였다.
3. 용어 사전 비즈니스 로직 구현 및 예외 정의 TermService를 통해 용어 조회 및 검색 기능의 비즈니스 로직을 구현하였으며, 조회 실패 시 예외 처리를 위한 TermNotFoundException 정의 및 ErrorCode에 TERM_NOT_FOUND(404, "존재하지 않는 용어입니다") 에러 코드를 추가하여 전역 예외 처리를 지원하도록 보완하였다.
4. API 데이터 전송 DTO 및 REST 컨트롤러 구현 용어 상세 조회를 위한 TermResponse와 용어 목록 조회를 위한 TermListResponse DTO 구조를 설계하였으며, TermController를 통해 클라이언트 요청을 처리할 REST API 엔드포인트를 구현하였다.
5. 초기 시드 데이터(Seed Data) 자동 적재 설정 프로젝트 실행 시 부동산 필수 핵심 용어 데이터를 데이터베이스에 자동으로 삽입하여 적재할 수 있도록 TermSeedDataLoader 클래스를 개발하였다.
6. API 응답 데이터 널 안전성(Null-safety) 및 버그 수정 TermResponse에서 용어의 위험 등급(RiskLevel)이 비어 있을 때(Null) 발생할 수 있는 NullPointerException을 방지하도록 3항 연산자 기반의 널 안전 처리를 추가하였고, TermService에서 용어 목록 반환 시 전체 요소 수(totalElements) 대신 현재 페이지의 요소 수(numberOfElements)를 반환하도록 API 필드 스펙 버그를 수정하였다.
7. feature/terms 브랜치 원격 반영 로컬에서 작업한 신규 기능 및 수정 내역을 정리하여 git add 및 커밋을 수행하였으며, 원격 저장소의 feature/terms 브랜치로 푸시하여 동기화 처리를 성공적으로 완료하였다.

---

## 💻 Backend 작업 로그

### - 백엔드 작업 로그

### 개요
부동산 용어 사전(Term) 관련 도메인 엔티티, Repository, Service, Controller 및 초기 데이터 로더를 신규 구축하였으며, API 응답 필드 수정 및 예외 처리, Null-safety 처리 보완 등 버그 수정 작업을 완료하여 백엔드 feature/terms 브랜치에 커밋 및 푸시를 완료하였다.

---

### 상세 작업 내역

1. 부동산 용어(Term) 도메인 엔티티 설계 부동산 용어 정보를 담을 RealEstateTerm 엔티티와 용어의 위험 등급을 나타내는 RiskLevel Enum, 그리고 용어 카테고리를 분류하는 TermCategory Enum을 신규 구현하였다.
2. 데이터베이스 연동용 Repository 구축 Spring Data JPA 기반의 RealEstateTermRepository 인터페이스를 신규 작성하여 데이터베이스 연동 및 기본 CRUD 기능을 활성화하였다.
3. 용어 사전 비즈니스 로직 구현 및 예외 정의 TermService를 통해 용어 조회 및 검색 기능의 비즈니스 로직을 구현하였으며, 조회 실패 시 예외 처리를 위한 TermNotFoundException 정의 및 ErrorCode에 TERM_NOT_FOUND(404, "존재하지 않는 용어입니다") 에러 코드를 추가하여 전역 예외 처리를 지원하도록 보완하였다.
4. API 데이터 전송 DTO 및 REST 컨트롤러 구현 용어 상세 조회를 위한 TermResponse와 용어 목록 조회를 위한 TermListResponse DTO 구조를 설계하였으며, TermController를 통해 클라이언트 요청을 처리할 REST API 엔드포인트를 구현하였다.
5. 초기 시드 데이터(Seed Data) 자동 적재 설정 프로젝트 실행 시 부동산 필수 핵심 용어 데이터를 데이터베이스에 자동으로 삽입하여 적재할 수 있도록 TermSeedDataLoader 클래스를 개발하였다.
6. API 응답 데이터 널 안전성(Null-safety) 및 버그 수정 TermResponse에서 용어의 위험 등급(RiskLevel)이 비어 있을 때(Null) 발생할 수 있는 NullPointerException을 방지하도록 3항 연산자 기반의 널 안전 처리를 추가하였고, TermService에서 용어 목록 반환 시 전체 요소 수(totalElements) 대신 현재 페이지의 요소 수(numberOfElements)를 반환하도록 API 필드 스펙 버그를 수정하였다.
7. feature/terms 브랜치 원격 반영 로컬에서 작업한 신규 기능 및 수정 내역을 정리하여 git add 및 커밋을 수행하였으며, 원격 저장소의 feature/terms 브랜치로 푸시하여 동기화 처리를 성공적으로 완료하였다.

---

---

## 🎨 Frontend 작업 로그

### - 프론트엔드 작업 로그

1. **Mockup 프론트엔드 기능 및 디자인 통합** C:\KOSTA_Projects\Mockup\front_zipt\frontend에 구축되어 있던 검증된 프론트엔드 스타일(tokens.scss, global.scss)과 이식 누락 상태였던 분석 결과, 마이페이지, 보증보험 시뮬레이터, 매물 비교 등 핵심 컴포넌트를 기존 구조를 깨뜨리지 않는 클래식 구조(src/components, src/pages)에 정교하게 머지 이식하였습니다.
2. **텍스트 정정 및 빈 뼈대 보완** 지도 페이지(MapPage.jsx) 및 Mock 데이터 내에 불일치하게 남아 있던 지민 지칭 단어를 "도연"으로 전면 정정하였으며, 회원가입(SignupPage), 게시판(BoardListPage, BoardDetailPage), 라우팅 보호(PrivateRoute), useAuthStore 등 0바이트 빈 파일 상태였던 부분에 Zustand 세션 동기화 및 뼈대 UI를 삽입하여 빌드 실패 위험을 사전 차단하였습니다.
3. **불필요한 폴더 및 대용량 HTML 정리** 프론트엔드 단독 저장소 명세에 맞지 않는 불필요한 백엔드 소스코드 폴더(backend/)와 중복 백업 디렉토리(frontend/, zipt/, pipeline/, screenshots/, docs/)를 로컬과 원격 Git 레포지토리 모두에서 완전히 영구 삭제하였습니다. 또한, 23MB에 달하는 대용량 standalone HTML 파일(ZIPT (standalone).html, ZIPT.html) 및 빌드 캐시를 지우고 .gitignore에 추가하여 저장소 용량을 최적화하였습니다.
4. **Vite Proxy 설정 추가** 프로젝트 루트 디렉토리에 vite.config.js 파일을 신규 작성하여, 프론트엔드 개발 서버에서 요청하는 /api 경로가 로컬에서 구동되는 스프링 부트 백엔드 서버(http://localhost:8080)로 크로스 도메인 및 세션 쿠키 유실 없이 안전하게 포워딩되도록 프록시 설정을 추가하였습니다.
5. **부동산 계약 꿀팁 게시판 구현** src/mocks/tipsData.js를 신규 생성하여 계약 전, 계약 중(특약), 계약 후(보증보험), 상식 등 카테고리별 꿀팁 데이터를 구축하였습니다. BoardListPage.jsx 및 BoardListPage.module.scss를 신규 설계하여 카드형 그리드 배치(Hover 시 상승 애니메이션 포함), 동적 카테고리 칩 필터링 및 실시간 검색, 그리고 카드 클릭 시 페이드인/스케일 업 되는 상세 뷰 모달 팝업 레이어를 구현하여 사용자 경험(UX) 완성도를 높였습니다. 아직 백엔드 API가 준비되지 않았을 경우에 대비해 로컬 Mock 데이터가 부드럽게 Fallback 노출되도록 예외 처리했습니다.
6. **빌드 검증 및 원격 반영** npm run build를 구동하여 167개 전체 모듈이 호환성 문제 없이 1.15초 만에 정상 컴파일됨을 검증하였으며, 모든 최종 정리 및 연동 결과물을 feature/integrate-frontend 브랜치에 실어 원격 저장소(https://github.com/1-ZIPT/front_zipt1.git)에 최종 푸시를 완료하였습니다.

---

