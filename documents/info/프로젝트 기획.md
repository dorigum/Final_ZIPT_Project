### 기술 스택

### 프로젝트 구조
#### Backend

인프라: S3+CloudFront / Redis / RDS / ElastiCache / GitHub Actions /CloudWatch+Grafana

```text
zipt/                                              ← 모노레포 루트
│
├── .github/
│   └── workflows/
│       ├── backend-deploy.yml                     ← Spring Boot EC2 자동 배포
│       ├── frontend-deploy.yml                    ← React S3 + CloudFront 배포
│       └── rag-deploy.yml                         ← FastAPI EC2 자동 배포
│
├── docker-compose.yml                             ← 로컬 전체 실행 (개발용)
│
├── zipt-backend/                                  ← Spring Boot 메인 백엔드
│   │
│   ├── build.gradle
│   ├── Dockerfile
│   ├── .env                                       ← 로컬 환경변수 (gitignore)
│   ├── .gitignore
│   │
│   └── src/
│       ├── main/
│       │   ├── java/com/zipt/
│       │   │   │
│       │   │   ├── ZiptApplication.java
│       │   │   │
│       │   │   ├── config/                        ← 전역 설정
│       │   │   │   ├── SecurityConfig.java        ← Spring Security + JWT + OAuth2
│       │   │   │   ├── WebConfig.java             ← CORS (CloudFront 도메인 허용)
│       │   │   │   ├── WebClientConfig.java       ← Clova OCR / 외부 API WebClient
│       │   │   │   ├── RestTemplateConfig.java    ← 국토부 / 환경공단 API
│       │   │   │   ├── RedisConfig.java           ← DB0(인증) / DB1(캐시) 분리
│       │   │   │   ├── S3Config.java              ← AWS S3 클라이언트 설정
│       │   │   │   └── SwaggerConfig.java
│       │   │   │
│       │   │   ├── domain/
│       │   │   │   │
│       │   │   │   ├── member/                    ← 회원 (이동혁)
│       │   │   │   │   ├── controller/
│       │   │   │   │   │   ├── AuthController.java       ← 회원가입 / 로그인 / 토큰 재발급
│       │   │   │   │   │   └── MemberController.java     ← 내 정보 / 수정 / 탈퇴
│       │   │   │   │   ├── service/
│       │   │   │   │   │   ├── AuthService.java          ← 로그인 / JWT 발급 / 로그아웃
│       │   │   │   │   │   └── MemberService.java        ← 회원 CRUD
│       │   │   │   │   ├── repository/
│       │   │   │   │   │   └── MemberRepository.java
│       │   │   │   │   ├── entity/
│       │   │   │   │   │   ├── Member.java
│       │   │   │   │   │   └── Role.java                 ← Enum (USER, ADMIN)
│       │   │   │   │   └── dto/
│       │   │   │   │       ├── SignUpRequest.java
│       │   │   │   │       ├── LoginRequest.java
│       │   │   │   │       ├── LoginResponse.java        ← AccessToken + RefreshToken
│       │   │   │   │       ├── TokenRefreshRequest.java  ← Refresh Token 재발급 요청
│       │   │   │   │       ├── MemberUpdateRequest.java
│       │   │   │   │       └── MemberResponse.java
│       │   │   │   │
│       │   │   │   ├── analysis/                  ← 전세사기 위험판단 (오혜진)
│       │   │   │   │   ├── controller/
│       │   │   │   │   │   └── AnalysisController.java   ← 등기부등본 분석 / PDF / 이력
│       │   │   │   │   ├── service/
│       │   │   │   │   │   ├── AnalysisService.java      ← 오케스트레이터
│       │   │   │   │   │   ├── OcrService.java           ← Clova OCR 호출
│       │   │   │   │   │   ├── MarketPriceService.java   ← 국토부 실거래가 / 공시가격 API
│       │   │   │   │   │   ├── LtvService.java           ← LTV / 보증보험 3공식 / 최종점수
│       │   │   │   │   │   ├── PdfExportService.java     ← iText PDF 생성 + S3 업로드
│       │   │   │   │   │   └── AnalysisCacheService.java ← Redis DB1 시세 캐싱
│       │   │   │   │   ├── repository/
│       │   │   │   │   │   └── AnalysisRepository.java
│       │   │   │   │   ├── entity/
│       │   │   │   │   │   └── AnalysisResult.java
│       │   │   │   │   └── dto/
│       │   │   │   │       ├── AnalysisRequest.java
│       │   │   │   │       ├── AnalysisResponse.java     ← LTV + 보증보험 + 점수 통합
│       │   │   │   │       ├── AnalysisHistoryResponse.java  ← 추가 (이력 카드용)
│       │   │   │   │       ├── RegistryData.java         ← OCR 추출 결과
│       │   │   │   │       ├── LtvResult.java            ← LTV 계산 결과
│       │   │   │   │       └── InsuranceResult.java      ← 보증보험 판별 결과
│       │   │   │   │
│       │   │   │   ├── contract/                  ← 임대차계약서 분석 (이동혁, 이정건)
│       │   │   │   │   ├── controller/
│       │   │   │   │   │   └── ContractController.java   ← 계약서 분석 / 층간소음 단독 조회
│       │   │   │   │   ├── service/
│       │   │   │   │   │   ├── ContractService.java      ← 오케스트레이터
│       │   │   │   │   │   ├── ContractOcrService.java   ← 계약서 OCR + 주소 추출
│       │   │   │   │   │   ├── NoiseService.java         ← 환경공단 API + 건축물대장 API (오혜진)
│       │   │   │   │   │   ├── NoiseCacheService.java    ← Redis DB1 층간소음 캐싱 (오혜진)
│       │   │   │   │   │   ├── RagLlmService.java        ← FastAPI RAG 서비스 HTTP 호출 
│       │   │   │   │   │   └── ChecklistService.java     ← 체크리스트 + 특약 생성
│       │   │   │   │   ├── repository/
│       │   │   │   │   │   └── ContractRepository.java
│       │   │   │   │   ├── entity/
│       │   │   │   │   │   └── ContractAnalysis.java
│       │   │   │   │   └── dto/
│       │   │   │   │       ├── ContractRequest.java
│       │   │   │   │       ├── ContractResponse.java     ← OCR + 층간소음 + 체크리스트 통합
│       │   │   │   │       ├── ContractOcrResult.java    ← 계약서 OCR 결과
│       │   │   │   │       ├── NoiseReport.java          ← 층간소음 최종 결과
|       |   |   |   |       ├── ContractHistoryResponse.java ← 이력관리
│       │   │   │   │       ├── NoiseTypeItem.java        ← 민원 유형별 통계
│       │   │   │   │       ├── RegionalNoiseStats.java   ← 환경공단 API 응답
│       │   │   │   │       └── BuildingInfo.java         ← 건축물대장 API 응답
│       │   │   │   │
│       │   │   │   ├── map/                       ← 인프라 지도 (구도연) — 비회원 허용
│       │   │   │   │   ├── controller/
│       │   │   │   │   │   └── MapController.java
│       │   │   │   │   ├── service/
│       │   │   │   │   │   ├── MapService.java
│       │   │   │   │   │   └── PropertyApiService.java   ← 부동산 외부 API 연동
│       │   │   │   │   └── dto/
│       │   │   │   │       ├── MapRequest.java
│       │   │   │   │       ├── InfraMarkerResponse.java
│       │   │   │   │       └── PropertyMarkerResponse.java
│       │   │   │   │
│       │   │   │   ├── board/                     ← 게시판 — 조회 비회원 / 작성 로그인
│       │   │   │   │   ├── controller/
│       │   │   │   │   │   └── BoardController.java
│       │   │   │   │   ├── service/
│       │   │   │   │   │   └── BoardService.java
│       │   │   │   │   ├── repository/
│       │   │   │   │   │   └── BoardRepository.java
│       │   │   │   │   ├── entity/
│       │   │   │   │   │   └── Post.java
│       │   │   │   │   └── dto/
│       │   │   │   │       ├── PostCreateRequest.java
│       │   │   │   │       ├── PostUpdateRequest.java
│       │   │   │   │       └── PostResponse.java
│       │   │   │   │
│       │   │   │   └── glossary/                  ← 용어사전 — 비회원
│       │   │   │       ├── controller/
│       │   │   │       │   └── GlossaryController.java
│       │   │   │       ├── service/
│       │   │   │       │   └── GlossaryService.java
│       │   │   │       ├── repository/
│       │   │   │       │   └── GlossaryRepository.java
│       │   │   │       ├── entity/
│       │   │   │       │   └── Glossary.java
│       │   │   │       └── dto/
│       │   │   │           └── GlossaryResponse.java
│       │   │   │
│       │   │   └── global/
        │   │       │
        │   │       ├── jwt/
        │   │       │   ├── JwtToken.java                   ← Access + Refresh Token DTO
        │   │       │   ├── JwtTokenProvider.java           ← 발급 / 검증 / 파싱
        │   │       │   └── JwtAuthenticationFilter.java    ← 요청마다 토큰 검사
        │   │       │
        │   │       ├── redis/
        │   │       │   ├── RefreshTokenService.java        ← DB0: Refresh Token CRUD
        │   │       │   └── TokenBlacklistService.java      ← DB0: 로그아웃 블랙리스트
        │   │       │
        │   │       ├── oauth/
        │   │       │   ├── CustomOAuth2UserService.java    ← 카카오·구글·네이버 처리
        │   │       │   ├── OAuth2SuccessHandler.java       ← JWT 발급 + CloudFront 리다이렉트
        │   │       │   └── OAuthAttributes.java            ← SNS별 응답 필드 통일
        │   │       │
        │   │       ├── s3/
        │   │       │   └── S3Service.java                  ← 파일 업로드 / presigned URL / 삭제
        │   │       │
        │   │       ├── rag/                                ← ★ FastAPI(Python) 전체 대체
        │   │       │   │
        │   │       │   ├── DocumentIndexer.java
        │   │       │   │   ├── [대체] pdf_loader.py        ← PagePdfDocumentReader (Spring AI)
        │   │       │   │   ├── [대체] index_runner.py      ← @PostConstruct 서버 시작 시 1회 실행
        │   │       │   │   └── PDF 로드 → 청크 분할 → 임베딩 → pgvector 적재
        │   │       │   │
        │   │       │   ├── RagRetrieveService.java
        │   │       │   │   ├── [대체] api/retrieve.py      ← vectorStore.similaritySearch()
        │   │       │   │   ├── [대체] core/vector_store.py ← Spring AI PgVectorStore
        │   │       │   │   └── 쿼리 → 임베딩 변환 → pgvector 코사인 유사도 검색
        │   │       │   │
        │   │       │   ├── RagGenerateService.java
        │   │       │   │   ├── [대체] api/generate.py      ← PromptTemplate + ChatClient 호출
        │   │       │   │   ├── [대체] core/llm_client.py   ← Spring AI ChatClient (Claude 자동연결)
        │   │       │   │   └── RAG 컨텍스트 + 건물정보 → 프롬프트 조립 → Claude API 호출
        │   │       │   │
        │   │       │   ├── RagProperties.java              ← top_k / similarityThreshold 설정값
        │   │       │   │
        │   │       │   └── [제거] main.py (FastAPI 서버)   ← Spring Boot 내부 호출로 대체
        │   │       │        [제거] core/embedder.py       ← Spring AI EmbeddingModel 자동 처리
        │   │       │
        │   │       ├── exception/
        │   │       │   ├── GlobalExceptionHandler.java     ← @RestControllerAdvice
        │   │       │   ├── ZiptException.java              ← 커스텀 예외
        │   │       │   └── ErrorCode.java                  ← 에러 코드 Enum
        │   │       │
        │   │       └── response/
        │   │           └── ApiResponse.java                ← 공통 응답 포맷 {success, message, data}
        │   │
        │   └── resources/
        │       ├── application.yml
        │       ├── application-dev.yml
        │       ├── application-prod.yml
        │       └── rag/                                    ← [대체] Python data/documents/
        │           ├── noise_report_2023.pdf               ← 이웃사이센터 연간보고서
        │           ├── molit_jeonse_guide.pdf              ← 국토교통부 전세 가이드
        │           └── hug_insurance_manual.pdf            ← HUG 보증보험 매뉴얼
        │
        └── test/
            └── java/com/zipt/
                ├── domain/
                │   ├── member/
                │   │   └── service/
                │   │       ├── AuthServiceTest.java
                │   │       └── MemberServiceTest.java
                │   ├── analysis/
                │   │   └── service/
                │   │       ├── AnalysisServiceTest.java
                │   │       └── LtvServiceTest.java
                │   └── contract/
                │       └── service/
                │           ├── ContractServiceTest.java
                │           └── NoiseServiceTest.java
                └── global/
                    ├── jwt/
                    │   └── JwtTokenProviderTest.java
                    └── rag/
                        ├── RagRetrieveServiceTest.java ← ★ 신규
                        └── RagGenerateServiceTest.java ← ★ 신규
```

#### docker-compose.yml 구성

```yaml
# 로컬 개발용 — 운영은 AWS 사용
services:
zipt-backend:        # Spring Boot :8080
zipt-rag:            # FastAPI    :8000
zipt-mysql:          # MySQL      :3306  → 운영: AWS RDS MySQL
zipt-postgres:       # PostgreSQL :5432  → 운영: AWS RDS PostgreSQL (pgvector)
zipt-redis:          # Redis      :6379  → 운영: AWS ElastiCache Redis
```

#### application.yml 핵심 구성 항목

```yaml
# application-prod.yml (운영)

# CloudFront CORS 허용
cors:
allowed-origins: https://zipt.com          ← CloudFront 도메인

# Redis — DB 번호로 용도 분리
spring.data.redis:
host: ${ELASTICACHE_HOST}
port:6379
  # DB0: JWT Refresh Token + 블랙리스트
  # DB1: 시세 API / 층간소음 API 캐시 (1시간)

# S3 — 버킷 2개
cloud.aws.s3:
bucket-frontend: zipt-frontend             ← React 빌드 (CloudFront 연결)
bucket-files:    zipt-files                ← 등기부등본 / 계약서 이미지

# FastAPI RAG 서비스 주소
rag.service.url: http://localhost:8000       ← 운영: EC2 내부 통신

# OAuth2 Redirect (CloudFront 도메인으로)
oauth2.redirect-uri: https://zipt.com/oauth/callback

# 모니터링 — Actuator Prometheus 엔드포인트 노출
management.endpoints.web.exposure.include: health,info,prometheus
```


#### GitHub Actions 배포 흐름

```text
.github/workflows/

backend-deploy.yml
  트리거: main 브랜치 push (zipt-backend/** 변경 시)
  1. Java 21 세팅
  2. Gradle 빌드 (bootJar)
  3. Docker 이미지 빌드
  4. ECR 푸시 (또는 EC2 직접 배포)
  5. EC2 SSH → Docker 재시작

frontend-deploy.yml
  트리거: main 브랜치 push (zipt-frontend/** 변경 시)
  1. Node.js 세팅
  2. npm ci + npm run build
  3. aws s3 sync ./build s3://zipt-frontend
  4. CloudFront 캐시 무효화

rag-deploy.yml
  트리거: main 브랜치 push (zipt-rag/** 변경 시)
  1. Docker 이미지 빌드
  2. EC2 SSH → Docker 재시작
```

#### API 엔드포인트

```text
[인증 — 비회원]
POST   /api/auth/signup
POST   /api/auth/login                       ← AccessToken + RefreshToken 발급
POST   /api/auth/refresh                     ← Redis DB0 조회 후 재발급
POST   /api/auth/logout                      ← Redis DB0 블랙리스트 등록
GET    /oauth2/authorization/{provider}      ← 카카오 · 구글 · 네이버

[회원 — 로그인 필요]
GET    /api/member/me
PUT    /api/member/me
DELETE /api/member/me

[전세사기 위험판단 — 로그인 필요 / 오혜진]
POST   /api/analysis/analyze                 ← 등기부등본 분석 (S3 이미지 저장 포함)
GET    /api/analysis/{id}/pdf                ← PDF 다운로드 (S3 presigned URL)
GET    /api/analysis/history

[임대차계약서 — 로그인 필요 / 이동혁·이정건]
POST   /api/contract/analyze                 ← 계약서 분석 + 층간소음 + 체크리스트
GET    /api/contract/noise?sigungu=강동구    ← 층간소음 단독 조회 (Redis 캐시 1시간)
GET    /api/contract/history

[지도 — 비회원 / 구도연]
GET    /api/map/infra
GET    /api/map/property

[게시판 — 조회 비회원 / 작성 로그인]
GET    /api/board
POST   /api/board
GET    /api/board/{id}
PUT    /api/board/{id}
DELETE /api/board/{id}

[용어사전 — 비회원]
GET    /api/glossary
GET    /api/glossary?keyword=전세

[FastAPI RAG — Spring Boot 내부 호출 전용]
POST   :8000/retrieve                        ← 벡터 유사도 검색
POST   :8000/generate                        ← LLM 해설 생성
```


### 데이터베이스

#### 1. 공통(member)

```sql
-- ============================================================
--  1. members (회원) 
-- ============================================================
CREATE TABLE members (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    email       VARCHAR(100) NOT NULL,
    name        VARCHAR(50)  NOT NULL,
    role        VARCHAR(20)  NOT NULL DEFAULT 'USER',   -- USER, ADMIN
    provider    VARCHAR(20),                            -- kakao, google, naver, NULL(일반)
    provider_id VARCHAR(100),                           -- SNS 고유 ID
    is_deleted  TINYINT(1)   NOT NULL DEFAULT 0,        -- 소프트 삭제
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP
                                      ON UPDATE CURRENT_TIMESTAMP,
 
    PRIMARY KEY (id),
    UNIQUE KEY uk_members_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```


#### 2. 전세 사기 위험 판단

```sql
============================================================
-- 2. analysis_results (전세사기 위험판단) 
-- =========================================================
zCREATE TABLE analysis_results (
    id                    BIGINT       NOT NULL AUTO_INCREMENT,
    member_id             BIGINT       NOT NULL,
 
    -- 매물 정보
    address               VARCHAR(300) NOT NULL,
    property_type         VARCHAR(20),                  -- 아파트, 빌라, 오피스텔
    building_year         INT,                          -- 건축연도
 
    -- 금액 (원 단위)
    deposit               BIGINT       NOT NULL,        -- 전세보증금
    market_price          BIGINT,                       -- 추정 시세
    official_price        BIGINT,                       -- 공시가격
    total_priority_amount BIGINT,                       -- 선순위 채권 합계
 
    -- LTV 결과
    ltv_ratio             DECIMAL(5,1),                 -- 87.1
    risk_level            VARCHAR(20),                  -- SAFE, WARNING, DANGER
    risk_score            INT,                          -- 5~100점
    score_grade           VARCHAR(20),                  -- PREMIUM, CAUTION, DANGER
 
    -- 보증보험 결과
    insurance_eligible    TINYINT(1),                   -- 가입 가능 여부
    hug_debt_ratio        DECIMAL(5,1),                 -- HUG 부채비율
    priority_ratio        DECIMAL(5,1),                 -- 선순위 채권 비율
    total_ltv             DECIMAL(5,1),                 -- 총 LTV
 
    -- 임대사업자
    is_rental_business    TINYINT(1)   NOT NULL DEFAULT 0,
    rental_type           VARCHAR(50),                  -- 장기일반민간임대
 
    -- 채권 목록 JSON
    -- [{"rank":1,"type":"근저당권","creditor":"국민은행","amount":144000000}]
    priority_bonds        JSON,
 
    -- 파일 URL (S3)
    registry_image_url    VARCHAR(500),                 -- 등기부등본 이미지
    pdf_url               VARCHAR(500),                 -- 분석 리포트 PDF
 
    created_at            DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
 
    PRIMARY KEY (id),
    CONSTRAINT fk_analysis_member
        FOREIGN KEY (member_id) REFERENCES members (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```


#### 3. 임대차계약서 분석
1. **전체 Entity 구조**

```text
Member
 └─ Contract - 사용자가 업로드한 임대차계약서 정보
			└─ ContractAnalysis - AI 분석 결과. 전체 위험도, 요약, 분석 상태 저장
						└─ ChecklistItem - 생성된 체크리스트 항목 하나
									└─ ChecklistEvidence - 해당 항목의 근거. 계약서 근거, RAG 근거, 참고 문서 출처 등
```

2. **테이블 구조**

```sql
// contract
id
user_id
original_file_name
file_url
contract_type
address
lessor_name (임대인)
lessee_name (임차인)
deposit_amount
monthly_rent
start_date
end_date
created_at
updated_at
```

```sql
// contract_analysis
id
user_id
original_file_name
file_url
contract_type
address
lessor_name
lessee_name
deposit_amount
monthly_rent
start_date
end_date
created_at
updated_at
analysis_results_id
```

```sql
// checklist_item
id
analysis_id
category
title
description
status
risk_level
reason
recommended_action
required_document
display_order
created_at
updated_at
```

```sql
// checklist_evidence
id
checklist_item_id
evidence_type
content
source_title
source_url
source_document_id
page_number
created_at
```

3. **Enum 설계**

```java
public enum AnalysisStatus {
    PENDING,
    PROCESSING,
    COMPLETED,
    FAILED
}
```

```java
public enum RiskLevel {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL,
    UNKNOWN
}
```

```java
public enum ChecklistStatus {
    PASS,
    CAUTION,
    DANGER,
    NEEDS_REVIEW,
    NOT_APPLICABLE
}
```

```java
public enum ChecklistCategory {
    CONTRACT_BASIC_INFO,
    LESSOR_VERIFICATION,
    PROPERTY_RIGHTS,
    DEPOSIT_PROTECTION,
    GUARANTEE_INSURANCE,
    SPECIAL_TERMS,
    PAYMENT_DAY,
    MOVE_IN_AFTER,
    CONTRACT_TERMINATION,
    ETC
}
```

```java
public enum EvidenceType {
    CONTRACT_TEXT,
    RAG_REFERENCE,
    RULE,
    USER_DOCUMENT,
    SYSTEM
}
```


4. **Contract**

```java
package com.example.lease.domain.contract.entity;

import jakarta.persistence.*;
import lombok.*;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.time.LocalDateTime;

@Entity
@Table(name = "contracts")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
@Builder
public class Contract {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // 실제 서비스에서는 User 엔티티와 연관관계 설정
    @Column(nullable = false)
    private Long userId;

    @Column(nullable = false)
    private String originalFileName;

    @Column(nullable = false)
    private String fileUrl;

    private String contractType; // JEONSE, MONTHLY_RENT 등으로 Enum 처리 가능

    private String address;

    private String lessorName;

    private String lesseeName;

    private BigDecimal depositAmount;

    private BigDecimal monthlyRent;

    private LocalDate startDate;

    private LocalDate endDate;

    @Column(columnDefinition = "TEXT")
    private String extractedText;

    private LocalDateTime createdAt;

    private LocalDateTime updatedAt;

    @PrePersist
    void prePersist() {
        this.createdAt = LocalDateTime.now();
        this.updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    void preUpdate() {
        this.updatedAt = LocalDateTime.now();
    }
}
```

1. **ContractAnalysis**

```java
package com.example.lease.domain.analysis.entity;

import com.example.lease.domain.contract.entity.Contract;
import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "contract_analyses")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
@Builder
public class ContractAnalysis {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    /**
     * 어떤 계약서에 대한 분석인지
     */
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "contract_id", nullable = false)
    private Contract contract;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private AnalysisStatus analysisStatus;

    @Enumerated(EnumType.STRING)
    private RiskLevel overallRiskLevel;

    @Column(columnDefinition = "TEXT")
    private String summary;

    /**
     * 사용한 LLM 모델명
     * 예: gpt-4o-mini, gpt-4.1-mini 등
     */
    private String modelName;

    /**
     * 프롬프트 버전
     * 분석 결과 재현성과 개선 이력 관리용
     */
    private String promptVersion;

    /**
     * RAG 지식베이스 버전
     */
    private String ragVersion;

    @Column(columnDefinition = "TEXT")
    private String failureReason;

    @OneToMany(
            mappedBy = "analysis",
            cascade = CascadeType.ALL,
            orphanRemoval = true
    )
    @Builder.Default
    private List<ChecklistItem> checklistItems = new ArrayList<>();

    private LocalDateTime createdAt;

    private LocalDateTime updatedAt;

    public void complete(RiskLevel overallRiskLevel, String summary) {
        this.analysisStatus = AnalysisStatus.COMPLETED;
        this.overallRiskLevel = overallRiskLevel;
        this.summary = summary;
    }

    public void fail(String failureReason) {
        this.analysisStatus = AnalysisStatus.FAILED;
        this.failureReason = failureReason;
    }

    public void addChecklistItem(ChecklistItem item) {
        checklistItems.add(item);
        item.setAnalysis(this);
    }

    @PrePersist
    void prePersist() {
        this.createdAt = LocalDateTime.now();
        this.updatedAt = LocalDateTime.now();

        if (this.analysisStatus == null) {
            this.analysisStatus = AnalysisStatus.PENDING;
        }
    }

    @PreUpdate
    void preUpdate() {
        this.updatedAt = LocalDateTime.now();
    }
}
```

6. **ChecklistItem**

```java
package com.example.lease.domain.analysis.entity;

import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "checklist_items")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
@Builder
public class ChecklistItem {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    /**
     * 어떤 분석 결과에 속한 체크리스트인지
     */
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "analysis_id", nullable = false)
    private ContractAnalysis analysis;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private ChecklistCategory category;

    /**
     * 체크 항목 제목
     * 예: 임대인과 등기부등본상 소유자가 일치하는가?
     */
    @Column(nullable = false)
    private String title;

    /**
     * 체크 항목 설명
     */
    @Column(columnDefinition = "TEXT")
    private String description;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private ChecklistStatus status;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private RiskLevel riskLevel;

    /**
     * 왜 이런 판단이 나왔는지
     */
    @Column(columnDefinition = "TEXT")
    private String reason;

    /**
     * 사용자가 해야 할 조치
     */
    @Column(columnDefinition = "TEXT")
    private String recommendedAction;

    /**
     * 추가로 필요한 서류
     * 간단히 문자열로 둘 수도 있고, 별도 테이블로 분리해도 됨
     * 예: 등기부등본, 건축물대장
     */
    private String requiredDocument;

    /**
     * 화면 출력 순서
     */
    private Integer displayOrder;

    /**
     * LLM이 생성한 원본 JSON 일부를 보관하고 싶을 때 사용
     */
    @Column(columnDefinition = "TEXT")
    private String rawAiResponse;

    @OneToMany(
            mappedBy = "checklistItem",
            cascade = CascadeType.ALL,
            orphanRemoval = true
    )
    @Builder.Default
    private List<ChecklistEvidence> evidences = new ArrayList<>();

    private LocalDateTime createdAt;

    private LocalDateTime updatedAt;

    protected void setAnalysis(ContractAnalysis analysis) {
        this.analysis = analysis;
    }

    public void addEvidence(ChecklistEvidence evidence) {
        evidences.add(evidence);
        evidence.setChecklistItem(this);
    }

    @PrePersist
    void prePersist() {
        this.createdAt = LocalDateTime.now();
        this.updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    void preUpdate() {
        this.updatedAt = LocalDateTime.now();
    }
}
```

7. **ChecklistEvidence**

```java
package com.example.lease.domain.analysis.entity;

import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDateTime;

@Entity
@Table(name = "checklist_evidences")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@AllArgsConstructor
@Builder
public class ChecklistEvidence {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    /**
     * 어떤 체크 항목의 근거인지
     */
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "checklist_item_id", nullable = false)
    private ChecklistItem checklistItem;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private EvidenceType evidenceType;

    /**
     * 근거 내용
     */
    @Column(columnDefinition = "TEXT", nullable = false)
    private String content;

    /**
     * 출처 제목
     * 예: 생활법령정보 - 주택임대차
     */
    private String sourceTitle;

    /**
     * 출처 URL
     */
    @Column(columnDefinition = "TEXT")
    private String sourceUrl;

    /**
     * VectorDB 또는 KnowledgeChunk의 ID
     */
    private String sourceDocumentId;

    /**
     * PDF 페이지 번호 등
     */
    private Integer pageNumber;

    /**
     * 검색 유사도 점수
     */
    private Double similarityScore;

    private LocalDateTime createdAt;

    protected void setChecklistItem(ChecklistItem checklistItem) {
        this.checklistItem = checklistItem;
    }

    @PrePersist
    void prePersist() {
        this.createdAt = LocalDateTime.now();
    }
}
```


---
#### Frontend



---
- **개발 시 주의 사항/정리**
    
    #### 법률 판단처럼 단정하지 않기
    
    서비스는 법률 상담이나 최종 보증보험 심사를 대신할 수 없다. 따라서 다음과 같은 표현을 사용한다.
    
    - `위험합니다` 보다 ➡️ **`위험 신호가 발견되었습니다`**
    - `가입 가능합니다` 보다 ➡️ **`입력된 정보 기준으로 가능성이 있습니다`**
    - `계약하면 안 됩니다` 보다 ➡️ **`계약 전 추가 확인을 권장합니다`**
    
    #### 답변 근거 표시
    
    RAG 답변에는 가능하면 다음 정보를 함께 표시한다.
    
    - 참고한 내부 용어
    - 참고 자료 제목
    - 자료 제공 기관
    - 최종 검토일
    
    #### 개인정보 보호
    
    - OCR 텍스트를 LLM에 보내기 전 개인정보를 마스킹한다.
    - 원본 문서의 저장 기간과 삭제 정책을 정한다.
    - 분석 로그에 주민등록번호나 계좌번호를 저장하지 않는다.
    
    #### 검색 실패 처리
    
    검색된 근거가 부족하면 LLM이 임의로 답변하지 않도록 한다.

```text
현재 등록된 자료만으로는 정확하게 판단하기 어렵습니다.
등기부등본의 갑구 또는 을구 내용을 다시 확인해 주세요.
```

---

ZIPT에서 RAG는 독립된 하나의 화면이 아니라, 다음 기능들이 동일한 내부 지식을 공유하도록 만드는 공통 기반 기술이다.

```text
	내부 용어 사전
            +
    위험 키워드 사전
            +
    공식 기준 및 행동 가이드
            ↓
    공통 RAG 검색 계층
            ↓
    용어 사전 / 서류 분석 / 챗봇 / 계약 체크리스트
```

가장 자연스러운 서비스 경험은 다음과 같다.

> 서류 분석에서 발견된 위험 키워드를 누르면 쉬운 설명과 행동 가이드가 열리고, 사용자가 이해되지 않는 부분을 챗봇에 추가로 질문할 수 있다.

이 구조로 구현하면 현재 분리되어 보이는 서류 분석, 용어 사전, 위험 키워드, 챗봇 기능이 하나의 일관된 사용자 여정으로 연결된다.